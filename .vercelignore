# LibreChat: root `api/` is the Express backend, not Vercel Serverless routes.
# Use a leading `/` so we only ignore the repo-root folder — NOT `packages/api`.
/api

/e2e
/playwright-report
/coverage
**/*.spec.js
**/*.spec.ts
**/*.test.js
**/*.test.ts
